/* =====================================================================
   НАЛАШТУВАННЯ. Заповнити один раз після створення проєкту в Supabase:
   Supabase → Project Settings → Data API (або API)
     Project URL      → SUPABASE_URL
     anon public key  → SUPABASE_ANON_KEY
   Ключ anon відкритий, його можна тримати в коді на GitHub:
   доступ до даних обмежують політики RLS у базі, а не цей ключ.
   ===================================================================== */

const SUPABASE_URL      = "https://ВАШ-ПРОЄКТ.supabase.co";
const SUPABASE_ANON_KEY = "ВАШ-ANON-КЛЮЧ";

/* Домен, що дописується до логіна. Змінювати не потрібно. */
const LOGIN_DOMAIN = "zdo.local";
